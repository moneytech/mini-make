#include "mk_parse.h"

int mk_parse(struct mk_state* state,
             const struct mk_token* token_array,
             size_t token_count) {

  (void)state;
  (void)token_array;
  (void)token_count;
  return 0;
}
